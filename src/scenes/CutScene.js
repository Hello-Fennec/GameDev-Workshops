import Phaser from "phaser";

class CutScene extends Phaser.Scene {
    constructor(test) {
        super({
            key: "CutScene",
        });
    }

    preload() {}

    create() {}

    update(delta, time) {}
}
export default CutScene;
